export const login = (email: string, password: string): string => {
    // Perform login logic (e.g., checking user credentials)
    return 'Sample JWT Token';
};

export const register = (email: string, password: string): string => {
    // Perform registration logic (e.g., saving user to the database)
    return 'User registered successfully';
};